---
const { href, ...props } = Astro.props;
const { currentLocale } = Astro;

const localizedHref = currentLocale && currentLocale !== 'en' 
  ? `/${currentLocale}${href.startsWith('/') ? href : '/' + href}`
  : href;
---

<a href={localizedHref} {...props}>
  <slot />
</a>